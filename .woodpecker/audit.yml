when:
  - event: push
    branch: main
  - event: pull_request

# cargo-audit scans Cargo.lock against the RustSec advisory database
steps:
  audit:
    image: rust:1
    commands:
      - cargo install cargo-audit --locked
      - cargo audit
